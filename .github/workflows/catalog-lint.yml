      - name: Debug + run lint
        run: |
          set -euxo pipefail
          pwd
          ls -la
          ls -la scripts || true
          command -v bash
          command -v python || true
          command -v python3 || true
          python3 --version || true
          bash scripts/catalog_lint.sh
